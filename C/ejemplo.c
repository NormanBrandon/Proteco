#include <stdio.h>
#include <string.h>
int caracterunico()
{
int arreglo(128);
if(strlen(palabra)>128)
	return 1;

	for(i=0;i<128;i++)
	arreglo[i]=0;
	}
	for (i=0;i<strlen(palabra);i++)
	{
	num(int)palabra[i];
	arreglo(num)=1;
	}
int main(void){


}
